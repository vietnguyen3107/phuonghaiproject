version: '3'

services: 
    db:

        image: mysql
        command: --default-authentication-plugin=mysql_native_password
        restart: always
        environment:
            MYSQL_ROOT_PASSWORD: phuonghaidb
            MYSQL_DATABASE: phuonghaidb
            MYSQL_USER: phuonghaidb
            MYSQL_PASSWORD: phuonghaidb
        ports: 
            - 3309:3306
